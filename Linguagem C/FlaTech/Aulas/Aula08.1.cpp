#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
#include<iostream>
#include<string.h>
#include<math.h>
#include<locale.h>
int main(int argc, char *argv[])
{
    setlocale(LC_ALL,"portuguese");
    //while e do
	/*int palito;
    printf("Digite o palito a ser usado: ");
    scanf("%d",&palito);
    while(palito<=20) //Executa o comando enquanto a condição for verdadeira
    {
    	printf("\nFogueira pode ser acesa com o palito %d",palito);
    	palito++;
	}
    do //excuta pelo menos uma vez o comando para dizer se é verdadeiro ou não
    {
    	printf("\nFogueira pode ser acesa com o palito %d",palito);
    	palito++
	}while(palito<=20); 
	printf("A caixa de fosforo continha %d palitos, resta um",palito);*/
	
	int a=2, b=5;
	a+=b; // é a mesma coisa que a=a+b e pode ser feita com qualquer operação
	printf("%d",a);
getchar();
printf("\n\n");
system("pause");
return(0);    
}
